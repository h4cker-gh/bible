case "$1" in
    frontend)
        cd frontend && npm run dev
    ;;
    backend)
        cd backend && npm run dev
    ;;
    *)
        echo "Usage: ./run [frontend|backend]"
        exit 1
    ;;
esac